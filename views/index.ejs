<!DOCTYPE html>
<html lang="en">
<%- include('components/head'); %>

    <body>
        <header>
            <%- include('components/header') %>

        </header>

        <section>

            <% categories.forEach(function(category, index) { %>
                    <div class="<%=index==0 ? 'is-active' : '' %>" data-page="<%= category._id %>">
                        <ul>
                            <% category.products.forEach(function(product, index) { %>
                                <li><%= product.title %></li>
                                <% }); %>
                        </ul>
                        
                    </div>
                    <% }); %>
                        <!-- <div class="is-active" data-page="1">
                        <$%- include('components/product') %>

                    </div> -->
                        <!-- <div data-page="2">
                <#%- include('components/products/snacks') %>
            </div>
            <div data-page="3">
                <#%- include('components/products/broodjes') %>
            </div>
            <div data-page="4">
                <#%- include('components/products/dranken') %>
            </div>
            <div data-page="5">
                <#%- include('components/products/handijs') %>
            </div> -->
        </section>

        <section class="popupDisabled" id="productPopup">
            <div class="popupScreen">
                <button class="closePopupButton">
                    <%- include('../static/icons/close') %>
                </button>
                <div class="popupHeader">
                    <img class="smallPicture" src="" alt="" data-product-image>
                    <h2 data-product-title>
                    </h2>
                </div>
                <form action="">
                    <input class="popupProductId" type="hidden" name="product_id" value="" data-product-id>
                    <input class="popupProductPrice" type="hidden" name="product_price" value="" data-product-price>
                    <%- include('components/counter') %>
                </form>
            </div>
        </section>

        <aside>
            <%- include('components/receipt') %>
        </aside>

        <!-- Latest compiled and minified JavaScript -->
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"
            integrity="sha384-aJ21OjlMXNL5UyIl/XNwTMqvzeRMZH2w8c5cRVpzpU8Y5bApTppSuUkhZXN0VxHd" crossorigin="anonymous">
            </script>
        <script src="../static/script/main.js"></script>
        <script src="../static/script/productChoices.js"></script>

    </body>

</html>